# hypixel-pit-auto-bullet-time
automatically switches to a bullet time sword and blocks when an arrow is (probably) going to hit the player

not very accurate, perhaps ping is a factor when blocking

built jar included
